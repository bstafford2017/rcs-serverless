{
  "name": "rcs-serverless",
  "version": "1.0.0",
  "description": "AWS lambdas for remote-concurrent-scheduler",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cross-env USERS_TABLE=Users tsc",
    "sls": "sls offline start",
    "deploy:getUser": "serverless deploy function -f getUser",
    "deploy:getUsers": "serverless deploy function -f getUsers"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.853.0",
    "cross-env": "^7.0.3",
    "uuid": "^8.3.2"
  }
}
